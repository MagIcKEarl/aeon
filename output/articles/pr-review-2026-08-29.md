# PR Review — aeonfun/aeon — 2026-08-29

**Mode**: review (per-PR deep review)

**Token constraint**: `gh` token has no write permissions (`pull: false`, `push: false`). Reviews could not be posted as PR comments. Findings documented here instead.

---

## PR #983 — trust cursor workspaces in headless runs
- **Author**: Svector-anu
- **Verdict**: approve-ready — no blockers
- **Head SHA**: `42189103f9e4cbe03ebb8da4f096280cfa19405`
- **Files**: `.github/workflows/ci-tests.yml`, `docs/harnesses.md`, `harness-adapter/README.md`, `harness-adapter/adapters/cursor.sh`, `harness-adapter/harnesses.json`, `scripts/tests/test_cursor_adapter.sh`
- **Additions/Deletions**: +54 / −5

**Review**:
Clean fix for the read-only cursor workspace trust gap. Adds `--trust` to both read-only and write invocations (fresh HOME = no persisted trust decision), keeps `--force` write-mode only. The three-layer change (adapter script, metadata/harnesses.json, docs) is consistent. The stub-agent regression test covers both modes correctly.

No CRITICAL or ISSUE findings.

---

## PR #984 — fail hermes runs on api errors
- **Author**: Svector-anu
- **Verdict**: approve-ready — no blockers
- **Head SHA**: `62644ed9950ed57852f3de18768cba5631125b77`
- **Files**: `.github/workflows/ci-tests.yml`, `harness-adapter/adapters/hermes.sh`, `scripts/tests/test_hermes_adapter.sh`
- **Additions/Deletions**: +54 / −0

**Review**:
Clean fix for false-green hermes runs (hermes exits 0 on HTTP 400/500). Adds word-boundary-anchored grep detection of HTTP 4xx/5xx in hermes stdout, emits real diagnostic on stderr, exits 1. Regression test covers both normal and zero-exit-api-error paths.

No CRITICAL or ISSUE findings.

---

## Summary

| PR | Title | Verdict | Findings |
|----|-------|---------|----------|
| #983 | trust cursor workspaces in headless runs | ✅ approve-ready | 0 |
| #984 | fail hermes runs on api errors | ✅ approve-ready | 0 |

Reviewed 2, skipped 0 (0 drafts, 0 bots, 0 dup-SHA, 0 bot-reviewed-recently).

**Note**: Reviews could not be posted to GitHub — token lacks write permissions (`pull: false`). Consider running with a `GH_TOKEN` that has `pull_request: write` scope, or merge manually after reviewing the above.